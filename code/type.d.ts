export declare type Status = 'Unhealthy' | 'Disconnected';
export interface Animal {
    height: number;
    weight: number;
}
export declare enum ClusterStatusEnum {
    removed = "Removed",
    unhealthy = "Unhealthy",
    healthy = "Healthy",
    disconnected = "Disconnected"
}
